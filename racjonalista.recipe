import re

from calibre.web.feeds.news import BasicNewsRecipe

class RacjonalistaRecipe(BasicNewsRecipe):
    __license__ = 'GPL v3'
    __author__ = u'intromatyk <intromatyk@gmail.com>'
    language = 'pl'
    version = 1

    title = u'Racjonalista'
    description = u'Portal propagatorów racjonalnego myślenia, prezentujący racjonalny obraz świata.'
    category = u'News'
    language = 'pl'
    cover_url=''
    remove_empty_feeds= True
    no_stylesheets=True
    oldest_article = 7
    max_articles_per_feed = 100000
    recursions = 0

    no_stylesheets = True
    remove_javascript = True
    simultaneous_downloads = 5

    keep_only_tags =[]
    keep_only_tags.append(dict(name = 'div', attrs = {'class' : 'title'}))
    keep_only_tags.append(dict(name = 'p', attrs = {'class' : 'strona'}))
    
   
    extra_css = '''
                    body {font-family: verdana, arial, helvetica, geneva, sans-serif ;}
                    div.title {text-align: left; font-size : large;}
                       '''    

    feeds          = [
                            ('Wszystkie Artykuly', 'http://www.racjonalista.pl/rss.php'),                    
                          ]
    
    def preprocess_html(self, soup):
        for alink in soup.findAll('a'):
            if alink.string is not None:
               tstr = alink.string
               alink.replaceWith(tstr)
        return soup
