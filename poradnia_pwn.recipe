import re

from calibre.web.feeds.news import BasicNewsRecipe

class PoradniaJezykowaRecipe(BasicNewsRecipe):
    __license__ = 'GPL v3'
    __author__ = u'intromatyk <intromatyk@gmail.com>'
    language = 'pl'
    version = 1

    title = u'Poradnia Językowa PWN'
    category = u'News'
    description = u'Artykuły z internetowej poradni językowej Wydawnictwa Naukowego PWN.'
    cover_url=''
    remove_empty_feeds= True
    no_stylesheets=True
    oldest_article = 7
    max_articles_per_feed = 100000
    no_stylesheets = True
    remove_javascript = True
    simultaneous_downloads = 5

    keep_only_tags =[]
    keep_only_tags.append(dict(name = 'h4'))
    keep_only_tags.append(dict(name = 'div', attrs = {'class' : 'pytanie'}))
    keep_only_tags.append(dict(name = 'div', attrs = {'class' : 'odpowiedz'}))

    extra_css = '''
                    body {font-family: verdana, arial, helvetica, geneva, sans-serif ;}
                    h4 {text-align: left; font-size: large;}
                       '''    

    feeds          = [
                            (u'Artykuly', u'feed://rss.pwn.pl/poradnia.rss'),
                          ]
    
    def preprocess_html(self, soup):
        for alink in soup.findAll('a'):
            if alink.string is not None:
               tstr = alink.string
               alink.replaceWith(tstr)
        return soup